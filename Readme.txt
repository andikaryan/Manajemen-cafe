A. Saat ingin menjalankan aplikasi perlu mengubah 
host,port,database,username,password di class CRUD sesusai pgadmin masing2

B. Membuat tabel pada database dengan nama tabel "menu" dengan kolom :
1. "id" (integer, not null, primary key, set type constraints menjadi identity)
2. "nama" (varchar(25), not null)
3. "harga" (integer, not null)
4. "kategori" (varchar(25), not null)

C. Untuk email dan password admin
Email:admin
Password:12345